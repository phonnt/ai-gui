# Copy to .env (never commit .env itself).
# Server port (vite proxies /api here).
AI_GUI_PORT=8787
# Agent runtime: sdk-first default (goal mode + true /clear + /fresh).
# rpc forces the OMP child (process isolation); auto behaves like sdk.
AI_GUI_RUNTIME=sdk
