# Copy to .env (never commit .env itself).
# Server port (vite proxies /api here).
AI_GUI_PORT=8787
# Agent runtime: auto (RPC-first, SDK fallback) | rpc | sdk.
# sdk unlocks goal mode + true /clear + /fresh; restart server to switch.
AI_GUI_RUNTIME=auto
